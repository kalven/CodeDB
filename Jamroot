project codedb
    : requirements
        <cxxflags>-std=c++0x ;

src = codedb options config build find init help regex file_lock search httpd serve serve_init serve_util database ;

libs = boost_filesystem boost_system pthread ;
lib $(libs) ;

exe cdb : $(src).cpp $(libs) ;
